# ===== pnpm 10 供应链 / 构建策略（Docker 内 --frozen-lockfile 需显式配置）=====
# 允许依赖安装脚本执行（esbuild 等需要 postinstall 下载平台二进制）
dangerously-allow-all-builds=true
# 关闭"最小发布年龄"策略：避免全新 lockfile 里的新版本包在 Docker 内被拒
minimum-release-age=0
minimum-release-age-strict=false